import React, { useEffect } from 'react';
import PropTypes from 'prop-types';

const ImageSliderBig = () => {

	return (
		<section id='images-slider-big' className='position-fixed d-flex flex-column justify-content-center align-items-center'>
			<p>BIG SLIDER IMAGE</p>
		</section>
	)
}

export default ImageSliderBig;
